import { CustoNumberPipe } from './custo-number.pipe';

describe('CustoNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new CustoNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
